#ifndef MAIN_H
#define MAIN_H

/* Only prototypes — no stdio helpers here */
char *create_array(unsigned int size, char c);

/* Provided by checker; you don't have to push _putchar.c */
int _putchar(char c);

#endif /* MAIN_H */
